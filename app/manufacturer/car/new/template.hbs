
<div class="container">
  <div class="panel panel-default">
    <form {{action "save" on="submit"}}>
      <div class="panel-heading">
        <h4>Add New Car for Mazda</h4>
      </div>
      <div class="panel-body">
        <div class="form-group">
          {{input type="name" value=formValues.model_name class="form-control" placeholder="Model Name"}}
        </div>
        <div class="form-group">
          {{input type="" value=formValues.year class="form-control" placeholder="Year"}}
        </div>
        <div class="form-group">
          {{input type="" value=formValues.horsepower class="form-control" placeholder="Horsepower"}}
        </div>
        <div class="form-group">
          <div class="input-group">
            <div class="input-group-addon">$</div>
            {{input type="" value=formValues.price class="form-control" placeholder="Price"}}
          </div>
        </div>
      </div>
      <div class="panel-footer">
        {{#link-to 'manufacturer.index' class="btn btn-info"}}Back{{/link-to}}
        <button type="submit" class="btn btn-warning pull-right">Submit</button>
      </div>
    </form>
  </div>
</div>
{{outlet}}
